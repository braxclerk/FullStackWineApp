export class User {
    id?: number;
    firstName?: string;
    lastName?: string;
    email?: string;
    dob?: Date;
    country?: string;
    role?: string;  // Assuming UserRole is an enum defined elsewhere
}